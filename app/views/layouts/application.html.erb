<!DOCTYPE html>
<html lang="da" itemscope itemtype="http://schema.org/Vandpibe">
<head>
	<meta charset="utf-8" />
	<%= render '/layouts/header' %>
 	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<%= stylesheet_link_tag    "application", :media => "all" %>
	<%= javascript_include_tag "application" %>
	<%= render '/layouts/google' %>
	<script type="text/javascript" src="http://maps.googleapis.com/maps/api/js?key=AIzaSyBEmB9qG5E3HPSKQ_Veb0ErystIyagHXNQ&sensor=false"></script>
  <%= csrf_meta_tags %>
</head>
<body>

<nav class="top-bar">
    <ul>
      <li class="name"><h1><a href="http://www.vandpibecafe.dk"><%=image_tag 'hookah.png', :width => 32%> Vandpibe Cafe</a></h1></li>
		<li class="toggle-topbar"><a href="#"></a></li>
	</ul>

	<section>
      <ul class="left">
		<li class="divider"></li>
        <li<% if current_path.index('debat') == nil && current_path.index('kontakt') == nil%> class="active"<% end %>><a href="/">Oversigt</a></li>
		<li class="divider"></li>
        <li<% if current_path.index('debat')%> class="active"<% end %>><a href="/debat" class="right-border">Debat</a></li>
		<li class="divider"></li>
      </ul>

      <ul class="right">
		<li class="divider hide-for-small"></li>
		<% if current_user.nil?%>
        <li><%= link_to "Login via Facebook", "/auth/facebook" %></a></li>
		<% else %>
        <li class="has-dropdown"><%= link_to image_tag(current_user.avatar_url, :class => 'avatar_url') + ' ' + current_user.nickname, "#", :class => "remove-right-border left-border" %>
			<ul class="dropdown">
				<li class="divider"></li>
				<li><%= link_to "Log af", logout_path %></li>
				<li class="divider"></li>
			</ul>
		</li>
		<% end %>
		<li class="divider"></li>
		<li<% if current_path.index('kontakt')%> class="active"<% end %>><a href="/kontakt" class="right-border">Kontakt</a></li>
      </ul>
	</section>
</nav>

<% if flash[:notice]%>
<div class="row">
	<div class="twelve columns">
		<div class="alert-box success">
			<%= flash[:notice]%>
  			<a href="" class="close">&times;</a>
		</div>
	</div>
</div>
<% end %>

<%= yield %>

<div id="myModal" class="reveal-modal [expand, xlarge, large, medium, small]">
  <p class="lead">Du bedes logge ind inden du vælger at bruge den handling.</p>
  <p><%= link_to "Login via Facebook", "/auth/facebook" %></p>
  <a class="close-reveal-modal">&#215;</a>
</div>

<% if current_user %>
<div id="logged_in" class="reveal-modal [expand, xlarge, large, medium, small]">
  <p class="lead">Du er nu logget på som: <br /><br /> <%= link_to image_tag(current_user.avatar_url, :class => 'avatar_url') + ' ' + current_user.nickname, "#", :class => "remove-right-border left-border" %><br /><br /></p>
<p><%= link_to "Luk vindue", "#", :onclick => "$('#logged_in').trigger('reveal:close');", :class => 'button' %></p>
  <a class="close-reveal-modal">&#215;</a>
</div>
<% end %>

<br /><br />

<script type="text/javascript">var addthis_config = {"data_track_addressbar":true};</script>
<script type="text/javascript" src="http://s7.addthis.com/js/250/addthis_widget.js#pubid=ra-4fd994d74ae6bfdc"></script>

</body>
</html>