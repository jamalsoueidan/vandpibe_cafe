<nav class="top-bar">
  <ul>
    <li class="name">
      <h1>
        <a href="#">
          Vandpibe Cafe
        </a>
      </h1>
    </li>
    <li class="toggle-topbar"><a href="#"></a></li>
  </ul>
  <ul>
    <li><%= link_to("Hjem", '/')%></li>
    <li><%= link_to("Debat", questions_path)%></li>
    <li><%= link_to("Topliste", toplist_path)%></li>
    <% if logged_in? %>
    <%= link_to image_tag(current_user.avatar_url, :class => 'avatar_url') + ' ' + current_user.nickname, "#", :class => "remove-right-border left-border" %> | <%= link_to "Log af", logout_path %>
    <% else %>
    <li><%= link_to("Log på", login_path)%></li>
    <% end %>
  </ul>
</nav>


<div class="text-center margin-bottom30"><%= link_to(image_tag('logo4.png'), '/', :alt => 'Vandpibe Cafe')%></div>
