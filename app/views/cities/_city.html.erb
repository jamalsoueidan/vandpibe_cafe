<li id="by_<%=city.name%>" class="<%=city.name%><%=current_class(city_path(city.name))%>"><%= link_to city.name.big, city_path(city.name), :title => "Vandpibe Cafe " + city.name, :alt => "Vandpibe Cafe " + city.name %></li>