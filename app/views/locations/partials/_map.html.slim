div id="map" style="height:250px;"

javascript:
	$(function() {
		init_map({city_id: '#{@location.city}', location_id: #{@location.id}, latitude: #{@location.latitude}, longitude: #{@location.longitude}, zoom: 17})
	});
