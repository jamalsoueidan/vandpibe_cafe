<% if current_user.nil?%>
 	<%= link_to image_tag('facebook-login.png'), "/auth/facebook" %>
<% else %>
	<a href="#" data-dropdown="#dropdown-1"><%= image_tag(current_user.avatar_url, :class => 'avatar_url') %> <%= current_user.nickname %><img src="/assets/down_arrow.png" alt="" width="25" style="position:relative;top:8px;"></a>
<% end %>